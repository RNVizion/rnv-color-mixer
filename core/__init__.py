"""
Core components for the RNV Color Mixer application.

This package houses the color-science and data-model modules: color
mixing math, palette import/export, color slots, color history, preset
palettes, color harmonies, the fine-tune dialog, the screen color
picker, the Package D control panel, and the image handler.

Submodules are imported directly by consumers — e.g.
``from core.color_math import ColorMath`` — so this package exposes
only metadata at the top level.
"""

__author__ = "RNV Development"
