# RNV Color Mixer — Runtime Dependencies
# Install with: pip install -r requirements.txt
#
# Python 3.10+ is required (the codebase uses `X | None` type syntax
# introduced in PEP 604).

PyQt6>=6.5.0,<7.0
Pillow>=10.0.0,<13.0
