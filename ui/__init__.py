"""
UI components for the RNV Color Mixer application.

This package houses the visual-layer modules: the canvas view for
image display and sampling, the UI handler for theme management, the
About dialog, the DebugButton (image-mode-aware QPushButton), and
the DebugOverlay for development diagnostics.

Submodules are imported directly by consumers — e.g.
``from ui.canvas_view import CanvasView`` — so this package exposes
only metadata at the top level.
"""

__author__ = "RNV Development"
